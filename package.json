{
  "name": "gonzalo-website",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^28.0.0"
  }
}
